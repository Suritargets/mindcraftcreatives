# Database (Neon Postgres)
# Haal deze op via: vercel env pull
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# Media uploads (Vercel Blob)
# Haal deze op via: vercel env pull
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."
